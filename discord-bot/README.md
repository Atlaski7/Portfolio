# Discord Bot

Put the Discord bot source code and its own dependencies in this folder.

The portfolio website lives separately in the project root under `app/` and
continues to run with `npm run dev`.

Keep bot secrets such as `DISCORD_TOKEN` in environment variables. Do not
commit them to this repository.
